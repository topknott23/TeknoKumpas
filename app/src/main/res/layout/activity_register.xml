<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/minimal_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="32dp">

        <FrameLayout
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginTop="32dp"
            android:background="@drawable/bg_minimal_icon_container"
            android:backgroundTint="@color/minimal_tint_teal"
            android:padding="16dp">
            <ImageView
                android:id="@+id/ivLogo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/ic_modern_compass"
                android:tint="@color/minimal_accent" />
        </FrameLayout>

        <TextView
            android:id="@+id/tvCreateAccount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:fontFamily="sans-serif-light"
            android:text="Create Account"
            android:textColor="@color/minimal_text_primary"
            android:textSize="32sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:orientation="vertical">

            <EditText
                android:id="@+id/etFullName"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:background="@drawable/bg_minimal_input"
                android:hint="Full Name"
                android:inputType="textPersonName"
                android:textColor="@color/minimal_text_primary"
                android:textColorHint="@color/minimal_text_secondary"
                android:drawableStart="@drawable/ic_modern_person"
                android:drawablePadding="16dp"
                android:drawableTint="@color/minimal_text_secondary"
                android:paddingStart="16dp"
                android:paddingEnd="16dp" />

            <EditText
                android:id="@+id/etStudentId"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_minimal_input"
                android:hint="Student ID"
                android:inputType="text"
                android:textColor="@color/minimal_text_primary"
                android:textColorHint="@color/minimal_text_secondary"
                android:drawableStart="@drawable/ic_modern_card"
                android:drawablePadding="16dp"
                android:drawableTint="@color/minimal_text_secondary"
                android:paddingStart="16dp"
                android:paddingEnd="16dp" />

            <EditText
                android:id="@+id/etPassword"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_minimal_input"
                android:hint="Password"
                android:inputType="textPassword"
                android:textColor="@color/minimal_text_primary"
                android:textColorHint="@color/minimal_text_secondary"
                android:drawableStart="@drawable/ic_modern_lock"
                android:drawablePadding="16dp"
                android:drawableTint="@color/minimal_text_secondary"
                android:paddingStart="16dp"
                android:paddingEnd="16dp" />

        </LinearLayout>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnSignUp"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="40dp"
            android:background="@drawable/bg_minimal_button"
            android:text="Sign Up"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="24dp"
            android:layout_gravity="center_horizontal"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Already have an account? "
                android:textColor="@color/minimal_text_secondary"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvLogin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Log In"
                android:textColor="@color/minimal_text_primary"
                android:fontFamily="sans-serif-medium"
                android:textSize="14sp" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>